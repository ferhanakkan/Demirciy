# references:
# * https://www.objc.io/issues/6-build-tools/travis-ci/
# * https://github.com/supermarin/xcpretty#usage

# osx_image: xcode11.0
# language: objective-c
# # cache: cocoapods
# podfile: Podfile
# before_install:
# - gem install cocoapods # Since Travis is not always on latest version
# - pod install
# script:
# - set -o pipefail && xcodebuild test -enableCodeCoverage YES -workspace Demirciy.xcworkspace -scheme Demirciy -sdk iphonesimulator13.0 ONLY_ACTIVE_ARCH=NO | xcpretty
# - pod lib lint

osx_image: xcode11
language: swift
xcode_project: Demirciy.xcodeproj
xcode_scheme: Demirciy
xcode_destination: platform=iOS Simulator,OS=13.0,name=iPhone X
podfile: Podfile
